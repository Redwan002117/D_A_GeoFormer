# Copy this file to .env and fill in your own Neon (or any Postgres) connection string.
# Never commit the real .env -- it's gitignored.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
